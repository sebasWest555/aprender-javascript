/*
determinar si un numero es par o impar
*/

let n = parseInt(prompt("como que no sabes si unnumero es par o impar pendejo a ver damelo: "));

if (n % 2 == 0) {
    console.log(`el numero ${n} es par`);
}else{
    console.log(`el numero ${n} es impar`); 
}